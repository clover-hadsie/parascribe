# GPU onnxruntime for deployment. Install: pip install -r requirements-gpu.txt
-r requirements.txt
# CUDA-12 build for Pascal sm_61 (GTX 1080 Ti); cuDNN 9 still supports sm_61.
# Do not float; see SPEC §8.
onnxruntime-gpu==1.24.4

# CUDA 12.4 runtime + cuDNN 9, shipped as pip wheels so the GPU stack is
# self-contained (no system CUDA/cuDNN needed). onnxruntime-gpu does not pull
# these itself. The userspace CUDA minor must not exceed what the host NVIDIA
# driver supports: a driver runs an equal-or-older CUDA runtime, never a newer
# one. These are pinned to the 12.4 line to match a 550-series driver (CUDA 12.4
# ceiling); newer drivers run them fine via backward compatibility.
nvidia-cudnn-cu12==9.1.0.70
nvidia-cublas-cu12==12.4.5.8
nvidia-cuda-runtime-cu12==12.4.127
nvidia-cuda-nvrtc-cu12==12.4.127
nvidia-cufft-cu12==11.2.1.3
nvidia-curand-cu12==10.3.5.147
nvidia-nvjitlink-cu12==12.4.127
