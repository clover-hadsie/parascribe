# GPU onnxruntime for deployment. Install: pip install -r requirements-gpu.txt
-r requirements.txt
# CUDA-12 build. Verified on a 3090 (CUDA 13 host driver) and pinned for the
# 1080 Ti (Pascal sm_61); cuDNN 9 still supports sm_61. Do not float; see SPEC §8.
onnxruntime-gpu==1.24.4

# CUDA 12 runtime + cuDNN 9, shipped as pip wheels so the GPU stack is
# self-contained (no system CUDA/cuDNN needed; the host driver runs these via
# backward compatibility). onnxruntime-gpu does not pull these itself.
nvidia-cudnn-cu12==9.23.2.1
nvidia-cublas-cu12==12.9.2.10
nvidia-cuda-runtime-cu12==12.9.79
nvidia-cuda-nvrtc-cu12==12.9.86
nvidia-cufft-cu12==11.4.1.4
nvidia-curand-cu12==10.3.10.19
nvidia-nvjitlink-cu12==12.9.86
